#include <stdio.h>

int main(void)
{
	int a, b, c, d, e;
	float f, g, h, i;

	printf("All the int variables %2f %f %f %f %f\n", a, b, c, d, e);
	printf("All the float variables %d %d %d %d\n", f, g, h, i);

	return 0;
}

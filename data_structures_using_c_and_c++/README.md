Solutions to some of the questions in Tenenbaum's Data Structures using C.

I have an include file "color.h" which has predefined macros for ASCII escape sequences. Some files won't compile without it
